# SVN_Repo_Update
Searches and findout svn repositories and update them with 'svn' command

use -p, --path option to pass a directory name where SVN Repositories are checked out

Example usage:
  py UpdateAllRepositories.py -p "D:\Projects\Radia\SVN"
